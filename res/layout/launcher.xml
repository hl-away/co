<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:padding="5pt"
              android:gravity="center_horizontal"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@drawable/bg_main00">
    <ImageView
            android:src="@drawable/logo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:contentDescription="@string/launcher.title"/>
    <TextView
            android:id="@+id/launcherText"
            android:gravity="center_horizontal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#ffffff"
            android:textSize="10pt"
            android:text="@string/launcher.text.internet_connection"
            android:paddingBottom="6pt"
            android:paddingTop="6pt"/>
    <Button
            android:id="@+id/launcherButton"
            android:paddingTop="7pt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="onClick"
            android:text="@string/launcher.button.connect"
            android:textSize="18dp"
            android:singleLine="false"
            android:textStyle="bold"
            android:textColor="#355a53" android:gravity="center_vertical|clip_vertical|fill_vertical"/>
</LinearLayout>